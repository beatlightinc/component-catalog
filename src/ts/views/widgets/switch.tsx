import * as React from 'react';
import * as classnames from 'classnames';

const Switch = (props: {
  onClick?: () => void,
  active?: boolean,
  color?: string
}) => {
  return (
    <div></div>
  );
};
